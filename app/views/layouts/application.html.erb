<!DOCTYPE html>
<html>
  <head>
    <title>MinimalList</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <section class="hero is-dark is-medium is-fullheight">
  <!-- Hero head: will stick at the top -->
  <div class="hero-head">
    <nav class="navbar is-dark">
      <div class="container">
        <div class="navbar-brand">
          <a class="navbar-item">
            <%= link_to 'NOTEPAD', root_path, class: "navbar-item" %>
            <!-- <img src="assets/images/logo.jpg" alt="Logo"> -->
          </a>
          <span class="navbar is-dark" data-target="navbarMenuHeroA">
            <span></span>
            <span></span>
            <span></span>
          </span>
        </div>
        <div id="navbarMenuHeroA" class="navbar-menu is-dark">
          <div class="navbar-end">
          <%= link_to "MAKE A NOTE", new_post_path, class:"navbar-item" %>
        </div>
        </div>
      </div>
    </nav>
  </div>

  <!-- Hero content: will be in the middle -->
  <div class="hero-body">
    <div class="container has-text-centered">
      <h1 class="title">
        SCROLL DOWN
      </h1>
      <h2 class="subtitle">
        to continue without distractions
      </h2>
    </div>
  </div>

  <!-- Hero footer: will stick at the bottom -->
<!-- Hero footer was removed -->
</section>
    <%= yield %>
  </body>
</html>
